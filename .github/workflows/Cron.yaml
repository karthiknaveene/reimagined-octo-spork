name: Cron

on:
  schedule:
    - cron: '*/6 * * * *'  # Every 5 minutes

jobs:
  run-scheduled-task:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run some scheduled task
        run: |
          echo "✅ This is a scheduled run every 5 minutes."
